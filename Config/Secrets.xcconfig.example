// Copy to Secrets.xcconfig or run scripts/configure-local-secrets.sh.
// The $() segment preserves // because xcconfig otherwise treats it as a comment.
SEENA_BACKEND_URL = https:/$()/seena-backend.example.vercel.app
SEENA_APP_TOKEN = replace-with-the-matching-vercel-token
